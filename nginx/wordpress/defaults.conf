# Should be included for most sites, as contains sensible defaults
# for file exclusions, security and static file caching.

# Exclusions
include wordpress/exclusions.conf;

# Security
include wordpress/security.conf;

# Static Content
include wordpress/static-files.conf;
